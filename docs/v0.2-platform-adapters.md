# v0.2 Platform Adapters

AgentPulse v0.2 adds two intentionally different integration levels.

## Claude Code

Claude Code is a precise integration based on documented command hooks. Hook
JSON arrives on stdin and is translated as follows:

| Claude event                      | AgentPulse status    |
| --------------------------------- | -------------------- |
| `SessionStart`                    | `running`            |
| `UserPromptSubmit`                | `running`            |
| `PreToolUse`                      | `using_tool`         |
| `PostToolUse`                     | `running`            |
| `PermissionRequest`               | `waiting_permission` |
| `Notification(permission_prompt)` | `waiting_permission` |
| `Notification(idle_prompt)`       | `waiting_input`      |
| `Stop`                            | `completed`          |
| `StopFailure(rate_limit)`         | `rate_limited`       |
| other `StopFailure`               | `failed`             |

`SessionEnd` is ignored. The hook itself has no access to current daemon state,
so emitting `idle` could replace a more useful completion or failure state.

The adapter reads only the fields needed for identity and a safe description.
It does not forward transcripts, tool inputs, or the complete payload.

See [Claude Code setup](setup-claude-code.md).

## Codex CLI notify

Codex is a narrow integration based on its documented external `notify`
program. That interface currently documents `agent-turn-complete`, which maps
to `completed`.

The adapter uses `thread-id` as the external session ID, `cwd` as project path,
and at most 200 characters of `last-assistant-message`. It ignores complete
`input-messages`.

This notify interface does not expose enough information to claim Codex
running, input-waiting, or permission-waiting state.

See [Codex setup](setup-codex.md).

## Codex hooks

v0.2.3 also supports documented Codex lifecycle hooks:

| Codex hook          | AgentPulse status    |
| ------------------- | -------------------- |
| `SessionStart`      | `running`            |
| `UserPromptSubmit`  | `running`            |
| `PreToolUse`        | `using_tool`         |
| `PermissionRequest` | `waiting_permission` |
| `PostToolUse`       | `running`            |
| `Stop`              | `completed`          |

The adapter is observation-only and returns no permission, context, input
rewrite, or turn-control output. It ignores prompts, transcripts, tool
input/output, assistant messages, and unknown payload fields. Users must review
and trust generated commands in Codex before they run.

See [Codex hooks setup](setup-codex-hooks.md).

## OpenCode plugin MVP

AgentPulse includes an OpenCode local-plugin integration for documented session,
permission, tool, and message events. The generated plugin uses an argv-style
`Bun.spawn` call and sends only whitelisted identity and status metadata.

The implementation is pending real local verification before it receives a
`supported` label. OpenCode Desktop remains `experimental`.

See [OpenCode setup](opencode.md).

## Experimental and research surfaces

Codex Desktop reuses Codex hooks with an explicit `surface=desktop` and remains
`experimental`. Antigravity provides only a sanitized `research-only` probe;
there is no stable setup command or support claim.

See [Codex Desktop](codex-desktop.md) and
[Antigravity research](antigravity.md).

## Delivery behavior

All ingest commands submit the normalized event to `POST /events`. Invalid
JSON, unsupported events, and daemon connection failures exit code zero so the
platform integration is non-blocking. Hook-style OpenCode, Codex lifecycle, and
Antigravity probe commands additionally keep stdout and stderr empty.

## Notifier outputs

The daemon supports `console`, `os`, and `none`. Existing notification policy
continues to notify only:

- `waiting_input`
- `waiting_permission`
- `completed`
- `failed`
- `rate_limited`

OS output is optional. Native notification runtime failures are reduced to
console warnings and do not fail ingestion.
